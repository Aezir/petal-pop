# 03 技术方案

> 2026-08 更新：完成 Godot vs Web 栈的正式选型评估，结论沿用 Web 栈；技术栈清单细化到库级。

## 1. 选型判断：要不要用 Godot？

**结论：不用。沿用 TypeScript + PixiJS + Tauri。**

判断的出发点不是"哪个技术强"，而是"这个游戏到底是什么"——

> Petal Pop 的核心画面是：一堆图片素材在画布上被拖拽、旋转、缩放、分层、加文字。
> 这不是动作游戏、不是物理游戏，**它本质是一个披着游戏皮的图层编辑器（迷你 Canva）**。
> 编辑器类软件是 Web 技术统治的领域，是游戏引擎的客场。

### 逐项对比（按本游戏的真实需求）

| 需求 | Web 栈 | Godot | 判定 |
|---|---|---|---|
| 图层编辑器交互（拖拽/旋转/层级/面板 UI） | DOM+CSS 做面板是主场 | Control 节点能做但繁琐 | **Web 胜** |
| 文字贴纸、中文排版（海报主题的核心） | CSS/canvas 文本 + 字体生态成熟 | 文本排版能力弱一档 | **Web 胜** |
| 撕取形变 + 镭射闪卡 shader | Pixi mesh + GLSL filter | Polygon2D + shader | 平手（都要手写） |
| 粒子、动态贴纸（二期） | Pixi 粒子够用 | 内置粒子编辑器更顺 | Godot 略胜 |
| 照片转贴纸（二期） | 浏览器有现成 wasm 抠图模型 | 基本要自己造轮子 | **Web 完胜** |
| 打包分发 exe | 需要 WebView2 运行时（见下方缓解） | 单文件自带运行时，发朋友零门槛 | **Godot 胜** |
| 你的成长路线（全栈工程师） | TS/React/渲染/状态管理全部可迁移 | GDScript 只在 Godot 里有用 | **Web 完胜** |
| AI 协作效率 | TS/Web 语料最多，AI 写得最准（AI 友好度的"语料杠杆"） | GDScript 语料少一个量级 | **Web 胜** |
| 手感调参 | 热重载 + tweakpane 调参面板，改参数即时生效 | Inspector 面板也很顺 | 平手 |
| 性能上限 | 浏览器有天花板，但本游戏远用不到 | 上限更高 | 不构成理由 |
| 未来上主机/移动端 | 移动可套 Capacitor，主机基本无路 | 导出管线成熟 | Godot 胜，但护栏里没有这项 |

**7 项定胜负的需求里 Web 拿 5 项**，且拿下的全是每天都要面对的（编辑器、文字、AI 效率、成长路线）；Godot 赢的两项，一项有缓解方案，一项是二期锦上添花。

### Godot 唯一的硬优势怎么化解

Godot 单文件 exe 确实是"发朋友"场景的真优势。Tauri 的缓解方案：**安装包内嵌 WebView2 引导安装器**（Tauri 打包选项勾一下），Win11 自带 WebView2、Win10 自动补装，朋友的体验同样是"双击安装完就玩"。已写进 05 分发计划。

### 什么情况下反悔换 Godot（切换触发条件，白纸黑字）

1. M0 原型在低配笔记本上掉帧，且优化两周无解（预期不会——这游戏渲染压力很小）
2. 玩法方向变了：要做实时动作、物理模拟类的核心玩法
3. 决定上主机（Switch 等）——护栏清单里目前没有这项

只要没触发，就不再重开这个话题。选型反复横跳是独立项目最大的隐形杀手。

## 2. 技术栈清单（库级）

| 层 | 选择 | 干什么 / 为什么 |
|---|---|---|
| 语言 | **TypeScript**（strict 模式） | 类型闭环：数据表、存档、状态全部有类型兜底，AI 写错立刻暴露 |
| 构建 | **Vite** | 热重载秒级，M0 调手感全靠它 |
| 画布渲染 | **PixiJS v8**（WebGL） | 贴纸、撕取 mesh 形变、闪卡 shader、书桌场景 |
| 界面 UI | **React** | 商店、抽奖、图鉴、贴纸盒这些面板用 DOM 做——编辑器软件的正统做法 |
| 状态管理 | **zustand** | 轻量状态库，游戏状态的唯一数据源；React 面板和 Pixi 画布都从它读写 |
| 数据校验 | **zod** | 贴纸表、掉率表、存档 JSON 加载时运行时校验，坏数据当场报错而不是玩到一半崩 |
| 音频 | **howler.js** | 撕纸声随机池、环境音循环 |
| 手感调参 | **tweakpane** | 开发用滑杆面板：撕取阻力、吸附距离、弹跳曲线实时调（发布版剔除） |
| 桌面壳 | **Tauri v2** | 打包 exe、本地存档读写；Rust 层用现成插件，不写 Rust |
| 测试 | **Vitest** | 只测纯逻辑：经济数值、掉率、存档版本迁移——不测画面 |
| 打包 | Tauri bundler → NSIS 安装包 | 内嵌 WebView2 引导安装 |

分工一句话：**画布里的一切归 Pixi，画布外的一切归 React，两边只通过 zustand 说话。**

## 3. 架构草图

```
┌───────────────────────────────────────┐
│ 桌面壳 Tauri（文件存档读写）             │
├───────────────────────────────────────┤
│ React 层（DOM）                        │
│   商店 / 抽奖 / 图鉴 / 贴纸盒 / 设置     │
├──────────────┬────────────────────────┤
│ zustand 游戏状态（唯一数据源）           │
│   拥有的贴纸·零花钱·各画布内容·任务进度   │
├──────────────┴────────────────────────┤
│ PixiJS 层（canvas）                    │
│   书桌场景 → 贴贴台编辑器 → 贴纸精灵     │
│   撕取 mesh / 闪卡 shader / 吸附辅助    │
├───────────────────────────────────────┤
│ 数据表 JSON + zod（贴纸/任务/掉率/价格） │
└───────────────────────────────────────┘
```

核心原则不变：**状态是唯一数据源，画面只是状态的投影**。存档 = 状态序列化成 JSON 写本地（双份写入，带 version 字段做迁移）。

## 4. 关键技术难点

| 难点 | 方案方向 |
|---|---|
| 撕贴纸形变手感 | Pixi mesh 顶点动画；M0 唯一要攻克的东西，tweakpane 调到爽为止 |
| 镭射闪卡 | fragment shader，鼠标位置作 uniform（与知识地图 WebGL/shader 同一棵树） |
| 统一贴贴台 | 编辑器核心与"主题"解耦：主题 = 画布底板 + 素材集 + 辅助规则开关 + 专属工具的注册包 |
| 桌面处处可贴 | 书桌本身就是一张常驻画布，复用编辑器，无特殊逻辑 |
| 房间父子跟随 | 贴在家具上的贴纸挂为家具子节点，Pixi 场景树天然支持 |
| 换装吸附辅助 | 底板标吸附点位，拖近时插值吸引；永远可无视（设计铁律） |
| 导出作品图片 | Pixi renderTexture → PNG |
| 存档防丢 | 双份写入 + version 字段迁移 |
| 照片转贴纸（二期） | 浏览器端 wasm 抠图（现成模型库）→ alpha 膨胀加白边 → 全本地，照片不出电脑 |

## 5. 目录结构（预定）

```
Petal_pop/
├─ docs/
├─ src/
│  ├─ desk/        # 书桌主场景（存钱罐/星星罐/书立/桌面画布）
│  ├─ editor/      # 统一贴贴台：选中工具、图层、吸附、裁切
│  ├─ themes/      # 海报 / 换装 / 房间——每个主题一个注册包
│  ├─ systems/     # 经济 / 任务 / 图鉴 / 存档
│  ├─ ui/          # React 面板：商店、抽奖、图鉴、贴纸盒
│  └─ data/        # 贴纸表、任务表、掉率表、价格表（JSON + zod schema）
├─ assets/         # 按 02-art.md 命名规范
└─ tools/          # 素材处理脚本（批量抠图、图集打包）
```

**数据驱动**不变：加新主题 = 加素材 + 填表 + 写一个主题注册包，编辑器核心不动。

## 6. 工程基建（第一周就做）

- [ ] `git init` + 首次提交（美术源文件大图用 Git LFS 或单独盘存）
- [ ] Vite + TypeScript(strict) + PixiJS + React + zustand 脚手架
- [ ] zod 数据表校验通道（先立规矩：所有 JSON 进游戏必过校验）
- [ ] 存档模块最先写（M0 就要能存进度）
- [ ] tweakpane 调参面板（M0 调手感的主力工具）

## 7. 当前网页版运行时（demo/）

正式版还没开工，`demo/` 是一份**没有构建步骤**的纯 ES module 实现（`demo/legacy.html` + `demo/js/*.js`），
用来把撕纸手感、素材包格式、存档迁移这些真问题先在浏览器里跑通。下面记的是它今天的样子。

### 背景锁死，画布 = 本子，两栏是浮层

整个窗口就是那张桌子，界面分三层：

1. **桌面背景：锁死。** `#world` 是一块写死 **1672 × 941** 的方形区域，桌面背景图铺在它身上，
   只按窗口做一次 **cover** 缩放：`fit = max(vw/1672, vh/941)`，居中，多出来的部分裁掉——
   所以永远铺满、四边不留空。`fit` 只在窗口大小变化时变，玩家怎么操作都动不了它。
2. **本子（画布）：能放大缩小、能拖。** `#boardWrap` 是一个 0×0 的锚点，位置 = 本子中心（`project.boardT`，桌面坐标），
   身上带 `scale(project.boardZ)`（0.25～4）。锚点是 0×0 的，所以缩放绕它的原点转就等于绕本子中心转。
   滚轮在桌面上滚缩放的是**本子**，背景一动不动；缩放时把鼠标底下那个"本子上的点"钉住不动，
   为此 `boardT` 也要跟着挪一点。工具行上的「100%」直接把 `boardZ` 设回 1，本子原地缩回去、不跑位。
3. **工具行和左右两个面板：浮层。** 三者都 `position:absolute` 在视口里，半透明 + 毛玻璃，盖在桌面上。
   开合（`state.ui.sideOpen` / `stripsOpen`）只是遮住或让开画面，**绝不挤压**桌面、本子和贴纸的位置与大小。

缩放本子走 `apply` 不走 `commit`：它不是对作品的改动，不打快照、不清重做栈。
（`boardZ` 存在作品里，所以撤销时可能被快照一起还原——这点可以接受。）

### 两套坐标

- **桌面坐标**：存档里 `on:'desk'` 的贴纸用它。屏幕 = 桌面 × `fit` + 世界层左上角偏移（`toStage` / `toScreen`）。
- **本子局部坐标**：`on:'board'` 的贴纸用它，原点是本子中心。桌面 = `boardT` + 局部 × `boardZ`（`stageToBoard` / `boardToStage`）。

**缩放只改看的大小，不改贴纸本身的大小**：贴纸的世界尺寸 = 原图尺寸 × 所在包的比例（768×512 规则，见 `strips.js`），
永远是那个数，`style.width` 写的就是它；屏幕上占多少像素由所在的层决定——
桌上的贴纸 `× fit`，本子上的贴纸 `× fit × boardZ`。

贴纸拿在手上的时候**一律用桌面坐标**（元素待在 `#layer` 里），松手那一刻才判断落在不落在本子上、要不要换算成局部坐标。
所以往放大的本子上贴，贴纸会在松手瞬间"长大"到本子的比例——位置不变，大小按本子算。

### 屏幕像素的撕纸层与命中检测

`#peel` 是一块 `position:fixed`、盖满整个窗口的 WebGL 画布（three.js），只画"正在撕的那一张"，
其余贴纸都还是普通 `<img>`。它里面的几何**全是屏幕像素**，因为撕纸的手感量（卷曲半径、最小拖动距离、边缘热区宽度）
本来就该按玩家看到的大小算，而不是按世界坐标算。

命中检测（`hitSticker`）也一样在屏幕像素里做，靠一个 `k` = **每世界单位多少屏幕像素**把两边接起来：
桌上的贴纸 `k = fit`，本子上的贴纸 `k = fit × boardZ`，贴纸条上的缩略图 `k = strips.thumbKOf(packId)`。
同一套撕法因此在画布和贴纸条上通用——从条上撕和从桌上撕走的是同一段代码。

判断"点到了谁"只看图案本身：透明的角落点不到，离轮廓边缘近的地方（`edge`）才撕得起来。

### 轮廓缓存

`silhouette.js` 把贴纸的 alpha 通道烤成两张表：哪里实心（`opaque`）、离最近的透明像素多远（`nearEdge`）。
- 缓存键是 **`` `${sha256}@${显示宽度}` ``**——同一张贴纸在条上和在画布上显示尺寸不同，就是两条不同的轮廓。
- 光栅化最长边封顶 **512** 像素：贴纸放到很大时按比例缩小了算，查询时再换算回去，省内存也省时间。
- 缓存是一个 `Map` 当 **LRU** 用，最多 **64** 条；连续缩放会不停产生新键，靠这个上限兜住。

### 贴纸条的"见缝插针"排版

右栏的贴纸条要像一张**真的印刷贴纸纸**：不是一张贴纸占一个隐形方块，而是小件钻进大件的透明角落和缝里。
`strips.js` 为此做的是一套**轮廓占用格**排版：

1. **烤掩膜**：每张贴纸按它在条上的显示尺寸画到画布上，按 `CELL = 2px` 一格切；
   一格里只要有一个像素 alpha ≥ 96 就算被占住。再**向外膨胀 1 格**——这一格就是贴纸之间的视觉间隙。
2. **方格纸**：条身是一张宽 `STRIP_INNER / CELL = 134` 格的方格纸，一行用 5 个 `Uint32` 存成位图，
   高度按需要往下长。碰撞检测 = 把掩膜按位移到目标列，和网格做几次**与运算**；
   再配一张"每行还剩几格空"的计数表，先排除肯定放不下的行，省掉逐列试位。
3. **先撒大的**：按实心格数排序，超过中位数两倍的算**锚**（大图）。
   按包围盒面积估个初始条高 `H0`（实测和最终条高只差 3%），把锚均匀铺在 `H0` 的各个高度档上，
   列位置按 `[靠左, 靠右, 居中]` 左右交错。档位分配用一个和锚数互质的黄金比例步长**打散**——
   照面积顺序直接排会变成"上面全是大图、越往下越小"。
4. **再塞小的**：其余贴纸按面积从大到小，从上到下、从左到右取第一个放得下的位置。
   因为碰撞是按掩膜算的，小件会自然钻进大件的透明角落。
5. 结果按 `stripLayout:<包id>:<清单哈希>:<条宽>:<缩略比例>:v2` 存进 kv，下次打开直接用。
   第一次打开先摆一个临时的按行排版，真排版算完（默认包 104 张约 **110ms**）再重排；用序号防止过期结果盖掉新的。

实测：默认包 104 张，条高从按行排的 2664px 降到 **1584px**，且没有任何两张贴纸的实心格重叠。

**代价**：包围盒现在会正当地互相重叠，所以"点到条上的哪一张"**不能看 `event.target`**。
`strips.slotsAt(x, y)` 只交出矩形罩住这个点的候选、最上面的排前面，由 `app.js` 逐个做透明度命中——
上面那张的透明角落会自然"漏"到下面那张。撕纸和悬停提示走的是同一条路。

这版不做倾斜摆放：要让贴纸歪着贴，每个角度都得单独烤一份掩膜，排版和命中检测都要跟着改。

### 撤销 / 重做

两条快照栈 `past` / `future`，存的是**当前作品整份 `structuredClone`**（不是 diff，作品数据很小，图靠引用）。
- `past` 最多 60 条；任何新改动清空 `future`。
- 快照打在**改动发生之前**，由 `commit(action)` 负责；连续同类动作（比如滚轮转贴纸）500ms 内合并成一次。
- 拖动类的快照特意**延后**到"真的动了"那一刻：
  挪本子在第一次 `pointermove` 时打，撕贴纸在揭过 `DETACH_AT` 整张离开的那一刻打（`pickUp` 之前）。
  这样"点一下本子"和"撕到一半放弃"都不算一次改动，也不会清掉重做栈。
- `state.ui`（两个面板的开关）和 `state.settings`、`state.packs` 都不在快照里——撤销只管作品。
